import TopBar from './TopBar';

const Example = () => {
  return (
    <TopBar title="제목" onFirstClick={() => {}} onSecondClick={() => {}}>
      <div onClick={() => {}}>Icon</div>
    </TopBar>
  );
};

export default Example;
